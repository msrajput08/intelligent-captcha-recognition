# Environment Variables for Resume Analyzer Docker Deployment
# Copy this file to .env and customize for your environment

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
DB_NAME=resume_analyzer
DB_USERNAME=postgres
DB_PASSWORD=ChangeThisPassword123!
DB_PORT=5432

# ==============================================================================
# LLM STUDIO CONFIGURATION
# ==============================================================================
# For Docker on Windows/Mac, use host.docker.internal to access host machine
# For Linux, use the host IP address or set up host.docker.internal
LLM_STUDIO_BASE_URL=http://host.docker.internal:1234/v1
LLM_STUDIO_API_KEY=not-needed
LLM_STUDIO_MODEL=mistralai/mistral-7b-instruct-v0.3
LLM_STUDIO_EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5

# Alternative models (uncomment to use)
# LLM_STUDIO_MODEL=meta-llama/llama-3.1-8b-instruct
# LLM_STUDIO_MODEL=microsoft/phi-3-mini-4k-instruct

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================
APP_PORT=8080
APP_VERSION=latest
SPRING_PROFILES_ACTIVE=docker

# ==============================================================================
# NGINX CONFIGURATION
# ==============================================================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ==============================================================================
# SECURITY (PRODUCTION)
# ==============================================================================
# Generate strong passwords for production!
# Use: openssl rand -base64 32

# Database password (production)
# DB_PASSWORD=<generate-strong-password>

# JWT Secret (for future authentication)
# JWT_SECRET=<generate-strong-secret>

# ==============================================================================
# RESOURCE LIMITS (PRODUCTION)
# ==============================================================================
# Uncomment and adjust for production deployments
# POSTGRES_MAX_CONNECTIONS=100
# APP_MAX_HEAP=2g
# APP_MIN_HEAP=1g

# ==============================================================================
# LOGGING
# ==============================================================================
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_IO_SUBBU_AI_FIREDRILL=DEBUG

# ==============================================================================
# FILE UPLOAD
# ==============================================================================
MAX_FILE_SIZE=50MB
MAX_REQUEST_SIZE=100MB

# ==============================================================================
# NOTES
# ==============================================================================
# 1. Never commit this file with real secrets to version control
# 2. Use different credentials for dev/staging/production
# 3. Rotate passwords regularly
# 4. Use secrets management tools (Vault, AWS Secrets Manager) in production
